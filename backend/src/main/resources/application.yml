spring:
  application:
    name: aihealthagent-api
  main:
    allow-circular-references: true
  
  # 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/demo_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: demo_user
    password: demo_pass_123
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  # Redis配置（暂时不使用）
  # redis:
  #   host: localhost
  #   port: 6379
  #   password:
  #   database: 0

# MyBatis配置
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.demo.aihealthagent.module.*.entity
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false

# 服务配置
server:
  port: 8080
  servlet:
    context-path: /

# 日志配置
logging:
  level:
    root: info
    com.demo.aihealthagent: debug
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n'
    file: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n'