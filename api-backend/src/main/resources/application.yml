server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/health_agent?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: health_user
    password: health_pass_123
    driver-class-name: com.mysql.cj.jdbc.Driver
  
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.aihealthassistant.entity
  configuration:
    map-underscore-to-camel-case: true

# 用于初始化内置用户的配置
init:
  user:
    admin:
      username: admin
      password: admin123456
    user:
      username: user
      password: user123456